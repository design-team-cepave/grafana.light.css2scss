# grafana.light.css2scss

## Install

install , run:

```
$ npm install && bower install
```

搬移 vendor components, run:

```
$ gulp components
```

啟動 local server:

```
$ gulp serve
```

發佈, run:

```
$ gulp build
```
